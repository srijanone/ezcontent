core.entity_form_display.paragraph.card_list.default:
  expected_config:
    content:
      view_mode_selection:
        type: options_select
  update_actions:
    change:
      content:
        view_mode_selection:
          type: fvm_options_select
